# Поток — Памятка для AI-агента

> Этот файл — в корне проекта. Обязателен для всех AI-агентов, работающих с
> кодовой базой. Нарушение стиля из раздела «Стиль кода» — критическая ошибка.

## Запуск и сборка

```bash
# Сборка (Windows, Go 1.23.4)
go mod tidy
go build -o bin/поток.exe ./src

# Тесты
go test ./src/... -v -count=1

# Запуск (требует Windows + прав администратора для глобального хука)
bin/поток.exe
```

> Платформа: **Windows-only**. Используются `user32.dll`, `gdi32.dll`,
> `Kernel32.dll` и библиотека `windigo`. Сборка/запуск на Linux невозможны.

## Технологии

- Язык: `Go 1.23.4`
- Фреймворк: `net` (стандартная библиотека) + `github.com/rodrigocfd/windigo` (Win32 UI)
- База данных: `none` (локальные файлы данных в `data/`)
- Шаблонизатор: `none` (UI — нативный Win32, не HTML)
- Логирование: `aoanima.ru/Logger` (dot-import)

## Стиль кода (обязательно)

Все идентификаторы — **на русском**, без сокращений:

```go
func ПолучитьПозициюКурсора() { ... }   // ✅
var БуферНажатий []ВиртуальныйКод        // ✅
// Неверно: getCaret() / мк *Буфер        // латиница / сокращение
```

- Вместо `error` — `СтатусОтвета{Код, Текст}` (пакет `aoanima.ru/QErrors`):
  коды `Ок`, `ОшибкаЗапроса`, `ОшибкаАвторизации`, `ВернутьОтветКлиенту` и др.
  Доп. коды Потока: `ОшибкаЗагрузкиРаскладки` (5100), `ОшибкаУстановкиХука` (5101),
  `ОшибкаПредсказания` (5102), `ОшибкаЗагрузкиДанных` (5103).
- Маркеры AI-кода: `// AiCopilot-Code начало` / `// AiCopilot-Code конец`
- Логирование: `Инфо()`, `ВыводОшибки()` из `aoanima.ru/Logger` (не `log.Println`).
- Комментарии на русском, поясняют «почему».
- Коммит после каждого изменения: `git commit -m "feat(predict): описание"`.

Полный стандарт — в `контракты/code-style.md` (рядом с этим файлом).

## Архитектура

```
Системный хук (WH_KEYBOARD_LL) → БуферНажатий → ПредсказаниеСлов (goroutine)
   → Trie (деревоСлов.go) + МодельNграмм/HMM (статистика.go) + Viterbi
   → каналПредсказания → ПотокОбновленияЮИ → ОкноПодсказок (windigo)
   → SendInput (вставка слова)
```

- Точка входа: `src/поток.go` (`main`, `ХукКлавиатуры`, `ПредсказаниеСлов`)
- Поток данных: хук → буфер → предсказание → канал → UI → инъекция
- Центральные типы: `ВиртуальныйКод`, `БуферНажатий`, `Трие` (`деревоСлов.go`),
  `МодельNграмм` (`статистика.go`)

## Известные проблемы / tech-debt

- `log.Println` в `поток.go` (~549, ~603) — заменить на `Logger` (см. code-style.md TD-1).
- `ЗажатаяКлавиша = make(map[bool]ВиртуальныйКод)` (`поток.go` ~830) — подозрительный
  тип, вероятно leftover (TD-2).
- `grid.go` — закомментированный мёртвый код, удалить (TD-3).
- `go copy.mod` — дубликат `go.mod`, удалить (TD-4).
- `bin/config.json` ссылается на `jetHTML` — мёртвая конфигурация (TD-5).
- Имя раскладки «Алвафит» исправлено на «Алфавит» во всех файлах (TD-6 ✅, см. code-style.md).

## Тесты

- Без внешних зависимостей (чистые функции Trie/МодельNграмм) работают всегда.
- Интеграционные (хуки, windigo, файлы данных) — только на Windows; помечать
  пропуском в коротком режиме.

## Чего нет / ограничения

- Нет СУБД, нет веб-API, нет авторизации/RBAC.
- Нет CI/CD.
- pprof на `:6060` — legacy, статус обсуждается (см. api-contract.md §5).
